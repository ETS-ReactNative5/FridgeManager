import React from 'react';
import { View, Text, TextInput, Button } from 'react-native';

const MyList = () => {
    return (
        <View>
            <Text>MyList works</Text>
        </View>
    );
};

export default MyList;
